# Cloudflare injects its own managed block above this one (Dashboard →
# Overview → "Manage your robots.txt"). That block already publishes
# `User-agent: * / Allow: /`, so repeating it here would create a duplicate
# group. We only add what Cloudflare does not: the sitemap location.
#
# Note: Cloudflare's managed block also blocks AI crawlers (GPTBot, ClaudeBot,
# Google-Extended, …). That is a deliberate dashboard setting, not something
# this file controls — change it there if you want to be citable in AI answers.

Sitemap: https://nexbridge-it.com/sitemap-index.xml
